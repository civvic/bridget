{
  "name": "@bridget/monorepo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "clean": "pnpm -r clean:all"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "@anywidget/types": "^0.2.0"
  }
}
